---
title: "bool"
description: "RandBox page: bool, includes examples and docs."
date: "2025-10-16"
tags: ["RandBox", "basics", "bool"]
keywords: ["RandBox", "basics", "bool", "random", "JavaScript", "MDX"]
---

# bool

```js
    // usages
    randBox.bool()
    randBox.bool({ likelihood: 30 })
```
Return a random boolean value (`true` or `false`).

```js
randBox.bool();
=> true
```

The default likelihood of success (returning `true`) is 50%.
Can optionally specify the likelihood in percent:

```js
randBox.bool({likelihood: 30});
=> false
```

In this case only a 30% likelihood of `true`, and a 70% likelihood of `false`.

