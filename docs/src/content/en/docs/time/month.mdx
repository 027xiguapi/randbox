---
title: "month"
description: "RandBox page: month, includes examples and docs."
date: "2025-10-16"
tags: ["RandBox", "time", "month"]
keywords: ["RandBox", "time", "month", "random", "JavaScript", "MDX"]
---

# month

```js
// usage
randBox.month()
```

Generate a random month.

```js
randBox.month();
=> 'January'
```

By default, returns just the full name.

Optionally specify raw to get the whole month object consisting of name,
short_name, and numeric.

```js
randBox.month({raw: true});
=> {name: 'October', short_name: 'Oct', numeric: '10'}
```
