# requirejs

Load RandBox with <a href="http://requirejs.org">RequireJS</a>

```js
  require(['RandBox'], function(RandBox) {
    // Instantiate
    var randBox = new RandBox();

    // Then use it:
    var my_random_integer = randBox.integer();
  });
```
